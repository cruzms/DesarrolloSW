import { CustomOnChangeDirective } from './custom-on-change.directive';

describe('CustomOnChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomOnChangeDirective();
    expect(directive).toBeTruthy();
  });
});
